<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login — Example</title>
  <style>
    /* Minimal styles for the example form */
    .field-error { color: #b00020; font-size: 0.9rem; margin-top: 0.25rem; }
    .invalid { outline: 2px solid rgba(176,0,32,0.12); }
    .form-row { margin-bottom: 0.75rem; }
    label { display: block; font-weight: 600; margin-bottom: 0.25rem; }
    input { width: 100%; padding: 0.5rem; border-radius: 6px; border: 1px solid #d0d7de; }
    button { padding: 0.5rem 0.9rem; border-radius: 6px; background: #2b6cb0; color: #fff; border: 0; cursor: pointer; }
    .container { max-width: 420px; margin: 48px auto; padding: 1rem; }
  </style>
</head>
<body>
  <div class="container">
    <form id="login-form" action="/login" method="POST" novalidate>
      <div class="form-row">
        <label for="username">Username</label>
        <input id="username" name="username" type="text" autocomplete="username" />
        <!-- inline error will be injected here by the script -->
      </div>

      <div class="form-row">
        <label for="password">Password</label>
        <input id="password" name="password" type="password" autocomplete="current-password" />
        <!-- inline error will be injected here by the script -->
      </div>

      <div style="margin-top:1rem;">
        <button type="submit">Log in</button>
      </div>
    </form>
  </div>

  <script src="validate-login.js"></script>
</body>
</html>
